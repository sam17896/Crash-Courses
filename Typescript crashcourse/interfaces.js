function showTodo(todo) {
    console.log(todo.title + ": " + todo.text);
}
var myTodo = { title: 'Trash', text: 'Take Out trash' };
showTodo(myTodo);
function showTodos(todo) {
    console.log(todo.title + ": " + todo.text);
}
var myTodos = { title: 'Trash', text: 'Take Out trash' };
showTodo(myTodo);
